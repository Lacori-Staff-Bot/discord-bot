import { buttons, Buttons } from "./buttons.js";
import { embeds, Embeds } from "./embeds.js";
import { modals, Modals } from "./modals.js";

const locales: { buttons: Buttons, embeds: Embeds, modals: Modals } = {
    buttons,
    embeds,
    modals
};
export default locales;